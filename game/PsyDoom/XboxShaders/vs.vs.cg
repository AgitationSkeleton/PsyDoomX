// Passthrough for a fullscreen quad: positions already in clip space.
struct vIn { float4 pos : POSITION; float2 tex : TEXCOORD0; };
struct vOut { float4 pos : POSITION; float2 tex : TEXCOORD0; };
vOut main(vIn I) { vOut O; O.pos = I.pos; O.tex = I.tex; return O; }
